from english_words import get_english_words_set
lst = get_english_words_set(['web2'], lower=True)
lst = list(lst)
count = 0
for i in lst:
    if "a" in i:
        count += 1





print(count/len(lst) * 100)
    


